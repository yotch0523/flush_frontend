{
  "watch": ["src"],
  "ignore": ["src/**/*.test.ts"],
  "ext": "ts,js",
  "exec": "etsc && cp -r src/config/locales dist/src/config/ && cp -r src/templates dist/src && TS_NODE_BASEURL=./dist node -r tsconfig-paths/register"
}
